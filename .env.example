# Copy this file to .env and fill in real values.
# NEVER commit the actual .env file.

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_NAME=restaurant_assistant
DB_USER=root
DB_PASSWORD=

# App
APP_SECRET_KEY=change_this_to_a_random_string
BCRYPT_ROUNDS=12

# Hugging Face (optional, only needed if you pull a gated model)
HF_TOKEN=
